# Zenapp App Template

This is the minimal runnable template used by Zenapp-generated apps.

Runtime contract:

- `npm install`
- `npm start`
- listen on `PORT`, defaulting to `8081`

The main editable product entry is:

- `app/index.tsx`

